class Animal {
    nome: string;
    porte: string;
    idade: number;

    constructor(nome: string, porte: string, idade: number) {
        this.nome = nome;
        this.porte = porte;
        this.idade = idade;
    }
}